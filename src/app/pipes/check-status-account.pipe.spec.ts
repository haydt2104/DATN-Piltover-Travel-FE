import { CheckStatusAccountPipe } from './check-status-account.pipe';

describe('CheckStatusAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckStatusAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
